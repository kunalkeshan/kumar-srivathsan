/**
 * External media asset URLs.
 *
 * Video files are hosted on Kumar's CDN rather than bundled with the repo to
 * keep the repository size small and avoid serving large binaries via Vercel.
 */
/** URL of the hero section background video (served from the CDN). */
export const heroVideoUrl =
  "https://cdn.kumarsrivathsan.com/website/aquamarine-ace.mp4"
